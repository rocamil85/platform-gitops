apiVersion: v1
kind: Service
metadata:
  name: platform-demo-backend
  labels:
    app: platform-demo-backend
spec:
  type: ClusterIP
  selector:
    app: platform-demo-backend
  ports:
    - name: http
      port: 80
      targetPort: http
